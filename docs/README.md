# Documentation

The written assignment report (PDF) is kept alongside the parent course folder as `Advanced-option-Assignment-final.pdf` when you submit; it is not bundled in this repository by default to keep the clone small. Add a copy here under `docs/` if your course requires the PDF inside the repo.
